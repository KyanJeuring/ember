# Ember

A dark color theme built around warm orange accents.

Ember is designed as an application-independent color palette with ports for
editors, terminals, and other developer tools.

## Structure

- `docs/` - visual previews and documentation
- `palette/` - canonical Ember color palette
- `ports/` - application-specific implementations
